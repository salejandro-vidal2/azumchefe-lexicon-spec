# azumchefe-lexicon-spec
This is the repository for the XML-based Azümchefe Lexicon file format (abbreviated as ALex). The repository defines the standard
for working with the format. It includes a schema file along with rule sets to keep in mind.

See [SCHEMA.md](/SCHEMA.md) to learn the schema's structure.
